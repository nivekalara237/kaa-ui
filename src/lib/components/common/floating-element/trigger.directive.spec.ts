import { TriggerDirective } from './trigger.directive';

describe('TriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new TriggerDirective();
    expect(directive).toBeTruthy();
  });
});
