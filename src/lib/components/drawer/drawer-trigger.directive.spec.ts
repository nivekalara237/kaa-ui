import { DrawerTriggerDirective } from './drawer-trigger.directive';

describe('DrawerTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
