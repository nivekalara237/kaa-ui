import { SwipeableDirective } from './swipeable.directive';

describe('SwipeableDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeableDirective();
    expect(directive).toBeTruthy();
  });
});
