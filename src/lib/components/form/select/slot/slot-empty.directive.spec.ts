import { SlotEmptyDirective } from './slot-empty.directive';

describe('SlotEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new SlotEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
