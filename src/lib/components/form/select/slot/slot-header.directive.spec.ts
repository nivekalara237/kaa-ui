import { SlotHeaderDirective } from './slot-header.directive';

describe('SlotHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SlotHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
