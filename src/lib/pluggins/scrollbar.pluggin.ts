import plugin from 'tailwindcss/plugin';

const scrollbarPlugin = plugin(({addUtilities, addComponents, addBase, e, theme, addVariant}) => {

  const scrollpros = {};
  
  addUtilities(scrollpros, {});
});
